## Review
- Correct: `npm run typecheck` and `npm test` both pass against the current unstaged worktree (229 tests passing). Compatibility exports in `src/git.ts` and `src/git-extras.ts` resolve under TypeScript, so I did not find broken imports/exports at typecheck time.
- Blocker: None found.
- Note (Medium): Stash pop/drop/current refresh failures can become unhandled promise rejections. `StashPickerController` invokes async callbacks without awaiting or attaching `.catch()` (`src/stash-picker-controller.ts:155-161`, `src/stash-picker-controller.ts:166-172`), while `runStashPop`, `runStashDrop`, and `runStashCurrent` perform a second `listStashes(...)` after the successful git operation outside `runStashOperation`'s try/catch (`src/viewer-stash-picker.ts:102-110`, `src/viewer-stash-picker.ts:125-147`). In the previous implementation, the confirmed pop/drop path was called with `.catch((error) => this.showAsyncError(error))` (`HEAD:src/viewer-stash-picker.ts:195-197`), so post-operation failures were surfaced instead of escaping. Smallest safe fix: make the viewer-provided stash callbacks catch and forward errors to `showAsyncError`, or add an error callback to `StashPickerController` and attach `.catch(...)` wherever it calls an async side-effect callback.
- Note (Low): Empty branch creation no longer reports the prior validation error. The new create-mode Enter path only calls `onCreate` when the trimmed name is truthy and otherwise does nothing (`src/branch-picker-controller.ts:115-119`); the previous viewer set `this.error = "Branch name is empty"` for the same case. Smallest safe fix: add an `onError`/validation callback or have the viewer validate before delegating so empty Enter restores the user-facing error.

Plan/progress files requested in the task were not present at `/home/nikolai/git/pi-git-tui/plan.md` or `/home/nikolai/git/pi-git-tui/progress.md` during review.
