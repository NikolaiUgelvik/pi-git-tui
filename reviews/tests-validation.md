## Review

- Correct: `npm test` passes all 229 tests. I also ran `npm run typecheck` and `npm run lint`; both passed. The new controller unit tests cover the extracted list/controller basics: opening/closing, search, navigation, selection callbacks, loading states, and rendering for branch (`tests/branch-picker-controller.test.ts:51`), command menu (`tests/command-menu-controller.test.ts:45`), commit picker (`tests/commit-picker-controller.test.ts:66`), stash picker (`tests/stash-picker-controller.test.ts:61`), worktree picker (`tests/worktree-picker-controller.test.ts:48`), and shared list state (`tests/list-state.test.ts:1`). Existing diff parser/display coverage is also still present via fixture-backed tests (`tests/diff-parser.test.ts:1`) and display tests (`tests/diff-display.test.ts:1`).

- Blocker (validation): `npm run fallow` currently fails, and `npm run check` includes fallow after biome/typecheck/tests (`package.json:26`). The failing findings include unused exports introduced or exposed by the split service modules, for example `getBranchName` (`src/git-branch-service.ts:43`), `getStagedDiff`/`getCommitRangeDiff` (`src/git-diff-service.ts:177`, `src/git-diff-service.ts:189`), `getCommitCount` (`src/git-history-service.ts:41`), `getStagedPaths` (`src/git-index-service.ts:117`), and `getWorktrees`/`switchWorktree` (`src/git-worktree-service.ts:36`, `src/git-worktree-service.ts:48`). Smallest safe fix: either remove unneeded exports/re-exports, import them through real runtime/test paths if they are intended API, or add the appropriate explicit fallow expectation only for deliberate public API surface.

- High (test gap): The new git service modules are mostly not tested by their actual exported functions. `tests/git-service.test.ts` duplicates implementation snippets inline instead of importing the real exports: it reimplements compacting/error logic (`tests/git-service.test.ts:26-35`, `tests/git-service.test.ts:80-135`) while the real functions live in `src/git-service.ts:28-40`, and it calls the fake `pi.exec` directly for the “git helper” and repository tests (`tests/git-service.test.ts:140-176`) instead of invoking `git()`/`ensureGitRepository()` (`src/git-service.ts:4-18`). A grep for service APIs in tests found no imports for many new functions such as `stageOrUnstageFile`, `toggleAllChangesStaged`, `runGitCommand`, `loadWorkingTreeDiff`, `generateCommitMessage`, or `runGitCommit`. Smallest safe fix: convert these tests to import the actual service functions and add fake-`ExtensionAPI.exec` tests for command args, cwd/root resolution, non-repo behavior, error formatting, and edge paths in `src/git-index-service.ts:85-157`, `src/git-diff-service.ts:110-205`, and `src/commit-message-service.ts:72-131`.

- Medium (test gap): The extracted controllers are covered, but most viewer integration paths that wire controllers to git services are not. `DiffViewer.handleInput` routes feature overlays and open inputs through the subclass chain (`src/viewer.ts:70-81`), and branch/stash/command/commit wrappers contain important async state transitions and refresh logic (`src/viewer-branch-picker.ts:61-142`, `src/viewer-stash-picker.ts:67-188`, `src/viewer-command-menu.ts:36-75`, `src/viewer-commit-picker.ts:26-95`). The only full `DiffViewer` workflow test in the suite is the worktree picker path (`tests/worktree.test.ts:119-142`). Smallest safe fix: add one fake-git `DiffViewer` test each for `b` branch switch/create, `s` stash apply/pop/drop or stash-current, `Ctrl+P` command menu refresh/error handling, and `c` commit selection, asserting git commands, overlay closure/state, refreshed document, and error/status output.

- Low (fixture quality): Several new fixtures are unused and some do not match the parsers they appear intended for. `grep` found no references to `branch-list`, `commit-list`, `stash-list`, or `worktree-list` under `tests/`. `getBranches` expects NUL-delimited `git branch --format` output (`src/git-branch-service.ts:7-15`), but `tests/fixtures/branch-list.txt:1-4` is whitespace-formatted. `getStashes` expects `%gd%x00%s` NUL-delimited lines (`src/git-stash-service.ts:15-22`), but `tests/fixtures/stash-list.txt:1-3` uses tabs. The worktree test uses an inline porcelain string (`tests/worktree.test.ts:26-39`) rather than `tests/fixtures/worktree-list.txt:1-12`. Smallest safe fix: either wire these fixtures into the relevant parser/service tests and align them to exact git output formats, or remove/replace unused fixtures to avoid false confidence.

- Note: `/home/nikolai/git/pi-git/plan.md` and `/home/nikolai/git/pi-git/progress.md` were requested but are not present, so this review is based on the unstaged worktree, tests, and package scripts only.
